recorder:
  purge_keep_days: 90
  include:
    domains:
      - sensor
      - switch
      - device_tracker
      - binary_sensor
      - light
      - alarm
#      - media_player
  db_url: !secret mysqldb
